[tool.black]
line-length = 120
extend_exclude = '''
(
.*migrations/
)
'''

[tool.isort]
profile = "black"
line_length = 120
sections = ['FUTURE', 'STDLIB', 'THIRDPARTY', 'FIRSTPARTY', 'LOCALFOLDER']
extend_skip_glob = ["*migrations/"]